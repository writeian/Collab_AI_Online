# AI Collab Environment Configuration
# Copy this file to .env and fill in your actual values

# Flask Secret Key (required for sessions and security)
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here

# AI Services (choose one or both)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Latest GA Sonnet (default in code). For max capability: claude-opus-4-7. For fastest/cheapest: claude-haiku-4-5
ANTHROPIC_MODEL=claude-sonnet-4-6

# AI Response Configuration (optional - defaults are sensible)
# Maximum tokens for AI responses (default: 350, increase if responses truncate too often)
AI_MAX_TOKENS=350
# Number of conversation turns to include as context (default: 6, one turn = user + assistant)
AI_MAX_HISTORY=6

# Per-Mode Token Overrides (optional - for modes that need more/less depth)
# Pattern: AI_MAX_TOKENS_{MODE_KEY_UPPERCASE}
# AI_MAX_TOKENS_DRAFT=500    # Draft mode needs more room
# AI_MAX_TOKENS_POLISH=350   # Polish mode is brief
# AI_MAX_TOKENS_EXPLORE=350  # Keep exploration concise

# Latency Optimization (safe defaults)
# Prompt caching (effective on repeated prefixes / multi-turn threads)
AI_PROMPT_CACHE_ENABLED=1
AI_PROMPT_CACHE_MIN_CHARS=1200

# Anthropic API request shaping
# Optional: set only if your SDK/backend supports it
# ANTHROPIC_SERVICE_TIER=auto      # auto | standard_only
ANTHROPIC_MAX_RETRIES=1           # avoid excessive retry stacking latency

# Speed routing for simple turns (4.6 models)
AI_SPEED_ROUTING_ENABLED=1
AI_SPEED_SIMPLE_THINKING_MODE=disabled   # disabled | adaptive_low
AI_SPEED_SIMPLE_QUERY_MAX_CHARS=160

# Retrieval gating (reduce hot-path work on normal chat turns)
AI_DOC_RETRIEVAL_ENABLED=1
AI_DOC_SEARCH_INTENT_ONLY=1
AI_DOC_SEARCH_MIN_QUERY_CHARS=24
# Library → AI context sizing (targeted retrieval vs full-document injection)
# Snippet cap per chunk in FTS/fallback paths; full-text cap when user asks to read the whole document
AI_DOC_SNIPPET_MAX_CHARS=3000
AI_DOC_FTS_MAX_CHUNKS=10
AI_DOC_FULL_TEXT_MAX_CHARS=120000

# Auto-note generation off request critical path
AI_AUTO_NOTES_ASYNC=1
AI_AUTO_NOTES_SYNC_FALLBACK=0
AI_AUTO_NOTES_ON_USER_MESSAGES=0
AI_AUTO_NOTES_QUEUE=ai_replies
AI_AUTO_NOTES_JOB_TIMEOUT=300

# Gunicorn tuning (stream-friendly defaults; keep gthread unless intentionally changed)
GUNICORN_WORKER_CLASS=gthread     # gthread | gevent | eventlet
GUNICORN_WORKERS=3
GUNICORN_THREADS=12
GUNICORN_TIMEOUT=300
GUNICORN_KEEPALIVE=5
GUNICORN_WORKER_CONNECTIONS=1000  # used by gevent/eventlet

# Google Services (optional - for Google Docs integration)
# Service account file path (default: service-account-key.json)
GOOGLE_SERVICE_ACCOUNT_FILE=service-account-key.json

# Google OAuth (optional - for user Google Docs access)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:5000/auth/google/callback 
