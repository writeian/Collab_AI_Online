[pytest]
testpaths = tests
# Network/live-API tests are deselected by default; run them with: pytest -m network
addopts = -m "not network"
markers =
    network: tests that make real network / AI-API calls (need API keys; deselected by default)
